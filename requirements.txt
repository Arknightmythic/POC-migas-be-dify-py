# =============================================================
# kai-dify — AI Document Processing Service
#
# CATATAN (2026-08-10): file ini sebelumnya berisi pin duplikat yang
# bentrok sehingga `pip install -r requirements.txt` gagal dengan
# "Double requirement given". Yang diperbaiki:
#   - numpy        : 2.3.2 vs 2.4.3   -> dipakai 2.4.3 (kompatibel opencv 4.13)
#   - ollama       : unpinned + 0.5.3 -> dipakai 0.5.3
#   - python-dotenv: unpinned + 1.1.1 -> dipakai 1.1.1
#   - requests     : 2.32.5 dua kali  -> satu baris
# Satu paket = satu baris. Jangan tambah entri unpinned untuk paket
# yang sudah dipin di bawah.
# =============================================================

# --- FastAPI framework ---
# [B-55] Semua DIPIN ke versi yang benar-benar teruji di venv.
# Sebelumnya paket-paket ini tanpa versi, dan itu bukan cuma soal kerapian:
# pip harus backtracking menelusuri puluhan versi untuk mencari kombinasi yang
# cocok, sehingga build image berjalan >10 menit tanpa selesai. Hasilnya juga
# tidak deterministik -- build hari ini dan besok bisa dapat versi berbeda.
fastapi==0.128.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32
aiofiles==25.1.0

# --- Core processing ---
PyMuPDF==1.28.2
Pillow==12.3.0
reportlab==5.0.0
# [B-55] docx2pdf DIHAPUS: sudah tidak dipakai kode mana pun (import-nya
# dibuang di B-20), dan paket itu mensyaratkan Microsoft Word -- tidak ada
# gunanya sama sekali di image Linux.

# --- Pinned dependencies ---
aiohappyeyeballs==2.6.1
aiohttp==3.12.15
aiosignal==1.4.0
annotated-types==0.7.0
anyio==4.10.0
attrs==25.3.0
certifi==2025.8.3
charset-normalizer==3.4.3
colorama==0.4.6
dataclasses-json==0.6.7
distro==1.9.0
frozenlist==1.7.0
greenlet==3.2.4
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
httpx-sse==0.4.1
idna==3.10
jiter==0.10.0
jsonpatch==1.33
jsonpointer==3.0.0
langchain==0.3.27
langchain-community==0.3.27
langchain-core==0.3.74
langchain-experimental==0.3.4
langchain-ollama==0.3.7
langchain-openai==0.3.31
langchain-text-splitters==0.3.9
langsmith==0.4.17
marshmallow==3.26.1
multidict==6.6.4
mypy_extensions==1.1.0
numpy==2.4.3
ollama==0.5.3
openai==1.101.0
orjson==3.11.2
packaging==25.0
propcache==0.3.2
# [B-54] Disinkronkan dengan venv yang benar-benar teruji. Menambahkan
# google-genai membuat pip meng-upgrade paket-paket di bawah, tapi
# requirements.txt tidak ikut diperbarui -- akibatnya `pip install -r`
# di image GAGAL total (ResolutionImpossible), mis. google-genai butuh
# httpx>=0.28.1 sementara di sini masih terpaku 0.25.2.
pydantic==2.13.4
pydantic-settings==2.10.1
pydantic_core==2.46.4
PyPDF2==3.0.1
python-dotenv==1.1.1
PyYAML==6.0.2
regex==2025.7.34
requests==2.32.5
requests-toolbelt==1.0.0
sniffio==1.3.1
SQLAlchemy==2.0.43
tenacity==9.1.2
tiktoken==0.11.0
tqdm==4.67.1
typing-inspect==0.9.0
typing-inspection==0.4.3
typing_extensions==4.15.0
urllib3==2.5.0
yarl==1.20.1
zstandard==0.24.0

# --- Google Gemini ---
# [B-14] Kode project ini sudah TIDAK memanggil `google.generativeai` lagi
# (sudah dimigrasi ke `google-genai`).
google-genai==2.17.0
#
# CATATAN: langchain-google-genai 2.0.10 masih mensyaratkan
# google-generativeai (>=0.8.0,<0.9.0), jadi paket EOL itu tetap ikut
# terinstall sebagai dependency transitif -- bukan karena kode kita.
# Dipakai untuk GoogleGenerativeAIEmbeddings di src/dify_processor/llm.py
# (yang saat ini hanya dibutuhkan oleh runner.py, dan runner.py belum
# dipanggil dari mana pun). Kalau nanti mau benar-benar bebas dari paket EOL:
# naikkan versi langchain-google-genai, atau lepas ketergantungan pada
# GoogleGenerativeAIEmbeddings.
#
# [B-55] DIPIN. Tanpa pin, pip menelusuri sampai versi 4.2.x lalu backtracking
# panjang karena batasan google-generativeai -- build tidak pernah selesai.
langchain-google-genai==2.0.10

# --- HTML -> DOCX pipeline ---
beautifulsoup4==4.14.3
lxml==6.0.2
python-docx==1.2.0
htmldocx==0.0.6
opencv-python==4.13.0.92
